# Where the Worker API lives. Local wrangler dev serves it on 8787.
# In the Pages project this is set to the deployed Worker's URL, and that origin
# must also appear in the backend's CORS_ORIGINS var.
VITE_API_BASE_URL=http://localhost:8787
